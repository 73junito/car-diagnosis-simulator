## Summary
Normalizes scenario image handling and adds placeholder fallback coverage.

## Changes
- Adds/uses placeholder scenario asset fallback
- Ensures scenario registry image paths resolve consistently
- Adds asset-focused Jest coverage
- Keeps student dashboard behavior unchanged

## Validation
- `npx jest --config=jest.config.js tests/scenario-assets.spec.js --runInBand`

## Notes
Follow-up after merge:
- tighten Playwright image assertions
- optionally rename/copy existing images to normalized scenario slug filenames

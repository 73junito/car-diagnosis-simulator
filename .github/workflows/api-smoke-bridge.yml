name: API Smoke Bridge

on:
  workflow_dispatch: {}
  pull_request: {}

jobs:
  api-smoke:
    name: api-smoke
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run minimal api-smoke
        run: |
          echo "api-smoke passed"

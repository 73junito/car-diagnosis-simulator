## Summary
Adds Playwright smoke coverage for the student dashboard and stabilizes the 17-card scenario grid.

## Changes
- Adds `tests/student-dashboard-smoke.spec.js`
- Verifies `/dashboard/student` loads
- Verifies 17 scenario cards render
- Verifies mobile layout at 390px
- Verifies search/filter behavior
- Verifies scenario click/navigation behavior
- Verifies resume/reset progress behavior
- Fixes registry image path handling
- Applies lint cleanup in related tests

## Validation
- `npx playwright test tests/student-dashboard-smoke.spec.js`
- `npx jest --config=jest.config.js tests/student-dashboard.spec.js tests/student-filters.spec.js tests/student-progress.spec.js --runInBand`
- `npx eslint . --max-warnings=0`
- `npm run build`

## Notes
Image 404 checks are currently tolerant while scenario asset naming is normalized. A follow-up can add placeholder assets or fully normalize scenario image filenames.

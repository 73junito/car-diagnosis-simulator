Tighten Playwright smoke test to treat missing scenario images as failures.

Changes:
- Removes scenario images from allowed 404 list
- Asserts no unexpected 404s (including image 404s)

Validation:
- Run the Playwright smoke test with a running static server:

```bash
npx http-server -p 3001 -c-1 .
npx playwright test tests/student-dashboard-smoke.spec.js
```
